.ui.menu.secondary

    .item 
        .ui.buttons 
            file-button.icon(on-read="import" type="text"): icon.upload Import
            btn.icon(on-click="export"): icon.download Export
    .item
        a.icon(href="sgw-example.json" newtab tooltip="Download the example project")
            icon.download.pink

    .item 
        .ui.buttons 
            ack-button.icon(on-click="prototypePrint" side="F.Cu, Edge" mirror="true" tooltip="Fabrication output of F.Cu")
                icon.print.red

            ack-button.icon(on-click="prototypePrint" side="B.Cu, Edge" tooltip="Fabrication output of B.Cu")
                icon.print.green


    //-file-button(on-read="importDXF2" type="text") DXF (2)

    //-
        .ui.button.icon(as-inline-popup="{position: 'right center'}"): icon.download Export
        .ui.popup.top.transition.hidden
            .ui.vertical.buttons 
                btn(on-click="exportSVG") SVG
                btn(on-click="exportDXF") DXF
                btn(on-click="exportJSON") JSON
                //- ack-button(on-click="exportKicad") Export Kicad_Pcb

    .item 
        .ui.input.right.labeled
            input(value="{{project.name}}")
            btn.icon.green.label(on-click="['export', project.name + '.json']")
                icon.download Export Project
    .item
        ack-button.icon(on-click="undo" tooltip="Undo changes") Undo

    .item 
        .ui.buttons 
            ack-button.icon(on-click="prototypePrint" side="F.Cu, Edge" scale="2" tooltip="TEST output of F.Cu"
                filename="Test_F.Cu" trace-color="lightgray")
                icon.print.red

            ack-button.icon(on-click="prototypePrint" side="B.Cu, Edge" mirror="true" scale="2" tooltip="TEST output of B.Cu" filename="Test_B.Cu" trace-color="lightgray")
                icon.print.green

    .item
        .ui.buttons
            btn.icon(
                on-click="save" 
                tooltip="Save now in local storage (just in case)"
                ): icon.save
            btn.icon(
                on-click="clear" 
                tooltip="Clear project"
                ): icon.erase
            
    .right.item 
        btn.icon(on-click="showHelp"): icon.help Help
